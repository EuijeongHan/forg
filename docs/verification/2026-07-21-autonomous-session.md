# 검증 기록 — 자율 작업 세션 (2026-07-21 오전, 원장 #34)

사용자 승인 하의 자율 작업 윈도우(~정오 KST). 각 PR은 CI green 확인 후 머지.

## PR별 요약

| PR | 내용 | 테스트 | 롤백 |
|---|---|---|---|
| #35 | LLM 일일 비용 가드(`LLM_DAILY_CALL_LIMIT`, 기본 500) · 봇 요약 캐시 · /start 법적 고지 | test_p1_quickwins 8/8 | revert 1회 |
| #36 | `Disclosure.raw_typed_data` 스냅샷(P1-2, additive 마이그레이션 0002) | test_typed_snapshot 4/4 | revert 1회(컬럼 잔존 무해) |
| #37 | events 정규화 계층 + `DisclosureEvent` 테이블 + `ENABLE_EVENT_CARDS`(기본 OFF) | test_events_foundation 20/20 | revert 1회(테이블 잔존 무해) |
| #38(예정) | 측정 스크립트 2종 + 본 문서 | 실측 실행 성공 | revert 1회 |

전체 스위트: **7파일 / 88케이스 green** (기존 48 + 신규 40).

## 한계 (정직 고지)
- **PG 마이그레이션 0002 로컬 실검증 불가** — Docker Desktop containerd 스토리지 손상
  (I/O 에러). 0001과 동일한 멱등 DO-block 패턴(0001은 PG 실검증 완료)이며,
  Railway 배포 로그 `DB 마이그레이션 적용 완료`로 최종 확인. **사용자 액션**:
  Docker Desktop 재시작(불가 시 Troubleshoot→Clean — 로컬 테스트 볼륨만 소실).
- 비용 가드·경보 카운터는 프로세스 메모리(재시작 시 리셋 — 허용).

## 실측 — DART 공시 볼륨 (최근 7일, list.json 읽기 전용)

```
전체 2,371건 (영업일 평균 ~590건/일)
대량보유(5%룰)       163건 —  6.9%  (영업일 평균 ~41건/일, 시장 전체)
임원·주요주주 소유    246건 — 10.4%  (~62건/일)
정정공시             531건 — 22.4%  (~133건/일)
현행 중요필터 매칭    345건 — 14.6%  (~86건/일)
```
*영업일 4일(7/15·16·20·21 오전) 기준. 7/18~19는 주말.*

### 해석 → 의사결정 입력
1. **P1-4(대량보유 추가) 판단 재료**: 시장 전체 기준 두 유형 합계 ~100건/일이지만,
   forG는 워치리스트 한정이라 체감 증가는 (워치 비율)배 — 예: 40종목 워치 시
   전체 상장사 대비 ~1.5% → **일 1~2건 수준 증가**로 추정. 알림 폭증 우려는 낮음.
   단, 5%룰 보고는 기관 자신의 매매도 포함되므로 인터뷰에서 수요·민감도 확인 후 결정 유지.
2. **정정공시 22.4%** — 공시 5건 중 1건이 정정. Stage 3(정정 비교)의 가치를 수치로
   뒷받침하는 강력한 근거.
3. **이상 관측**: 7/17(금) 0건 — 공휴일(제헌절)이 아닌 평일이라면 이상 신호.
   휴장일 여부 확인 필요. P0-3 자가 경보가 잡도록 설계된 바로 그 패턴.

## 재현
```bash
python scripts/measure_disclosure_volume.py 7   # .env의 DART_API_KEY 사용
```
