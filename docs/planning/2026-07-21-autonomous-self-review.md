# 자율 세션 자기검증 — 무엇을 의심하고 무엇을 믿어도 되는가

- 작성: 2026-07-21 (자율 세션 말미) · 원장: #34
- 목적: 사용자의 정오 검토를 돕기 위해 **내가 만든 것을 내가 먼저 비판**한다.
  각 변경의 신뢰도와 "되돌릴 후보" 여부를 정직하게 표기. 신뢰도는 검증 강도 기준이다.

신뢰도: 🟢높음(실행 검증+회귀) / 🟡중간(로직 검증, 실환경 미검증) / 🔴주의(가정 있음)

---

## 이미 머지되어 프로덕션에 영향 (되돌리려면 revert)

### #23 중복발송 스키마 + 요약 재호출 제거 — 🟢
- 근거: SQLite 10/10 + **실제 PostgreSQL 마이그레이션 검증**(제약 교체·데이터 보존·멱등).
- 의심점: 프로덕션 DB에 `seen_disclosures_receipt_no_key`가 **정확히 그 이름**이라는 가정.
  PostgreSQL 기본 명명 규칙이라 거의 확실하나, 배포 로그에서 마이그레이션 성공을 확인해야 100%.
- 되돌릴 후보? **아니오.** 가장 중요한 안정성 수정.

### #31 파이프라인 가드 (자정 창·자가 경보) — 🟡
- 근거: 행동 테스트 10/10. **단 CI가 시간 의존 버그를 잡아 #33으로 수정**(정직한 이력).
- 의심점: ① 2일 창 조회로 API 응답량 ~2배(rcept_times는 오늘만이라 전일 야간경고 뱃지 누락 가능 —
  알림 자체는 발송). ② 경보 임계(5회/10사이클)가 실환경에 적절한지는 미검증 — 너무 둔감/민감할 수 있음.
- 되돌릴 후보? 자정 창은 유지 권장(누락 방지). 경보 임계는 실운영 보고 튜닝.

### #35 LLM 비용 가드·요약 캐시·/start 고지 — 🟡
- 근거: 8/8. 의심점: ① 기본 한도 500이 실사용 대비 적절한지 미검증(env로 조정 가능). ② `_summary_cache`
  프로세스 메모리 — 다중 인스턴스 미공유(현재 단일이라 무방). ③ **/start 약관 링크가 GitHub 문서 초안**을
  가리킴(운영자 정보 미기입) — 정식 게시 URL로 교체 전까지는 임시.
- 되돌릴 후보? 캐시·가드는 유지. **약관 링크는 게시 URL 확정 시 반드시 교체**.

### #36 정형 스냅샷 (raw_typed_data) — 🟡
- 근거: SQLite 4/4. **의심점(중요): PG 마이그레이션 0002를 로컬에서 실검증 못 함**(Docker 스토리지 손상).
  0001과 동일 패턴이라 위험 낮지만, **배포 로그 확인이 필수**. 실패해도 additive라 악화는 없음(컬럼만 없음).
- 되돌릴 후보? 아니오. 단 배포 후 컬럼 생성 확인.

### #37 이벤트 정규화 계층 (플래그 OFF) — 🟢
- 근거: 20/20(단위·저장·멱등·플래그). 플래그 OFF 기본이라 **알림 불변**.
- 의심점: normalizer 필드 키는 프로덕션 `format_typed_disclosure`에서 가져왔으나, 정형 API 실응답과
  100% 일치하는지는 실데이터로 재확인 권장(카드에 이미 쓰이던 키라 위험 낮음).
- 되돌릴 후보? 아니오(꺼져 있음).

### #38 측정 스크립트 + 실측 — 🟢
- 근거: 실제 DART 7일 실행 성공. 의심점: 7/17(금) 0건 — **데이터 이상인지 휴장인지 미확인**.
  라벨 매칭("소유상황" 등)이 DART 실제 report_nm과 정확히 맞는지 소폭 불확실(측정용이라 영향 경미).
- 되돌릴 후보? 아니오(읽기 전용).

## 로컬 미머지 (프로덕션 무영향, push 대기)

### stage3 정정 비교 기반 — 🟡
- 근거: 17/17. **의심점(설계): 규칙 기반 원본 매칭**은 같은 날 동일 제목 복수 공시에서 오연결 가능 →
  그래서 confidence="rule"·"추정 연결" 표기로 방어. 파이프라인 미연결이라 지금은 무해.
- 검토 시: 매칭 규칙(접두어 제거 제목 동일+선행 접수)이 실제 정정공시 명명과 맞는지 실데이터 확인 필요.

### stage7 결정론 검증 — 🟡
- 근거: 20/20(단위 환각 검출·과잉거부 방지 포함). **의심점: 투자의견 금칙 패턴이 좁게 잡혀**
  일부 완곡한 의견을 놓칠 수 있음(과잉거부보다 과소검출 쪽으로 의도적 편향 — 정상 발송 막지 않기 위해).
  교차검증 임계(1e6)와 정형 필드 매칭은 exact Decimal — 반올림/표기 차이가 있으면 오검 가능.
- 미연결이라 무해. 파이프라인 연결(Stage 7 본계약) 전 실요약 샘플로 임계·패턴 튜닝 필요.

### docs (README·설계도) — 🟢
- 사실 기술. 의심점: 설계도의 "결정 대기"는 내 제안이지 확정이 아님(사용자·인터뷰 입력 필요).

## 세션 전반에서 내가 가장 불편한 것 (우선 검토 권장)
1. **PG 마이그레이션 0002 로컬 미검증** — Docker 손상 탓. 배포 로그로 반드시 확인.
2. **/start 약관 링크가 초안 URL** — 실제 게시 전엔 미완성 링크 노출.
3. **자가 경보 임계값** — 근거 있는 추정이나 실운영 데이터로 조정 필요.
4. **git push 장애로 후반 PR 지연** — 코드가 아니라 전달의 문제. 로컬엔 안전 보관.

## 내가 지킨 것 (믿어도 되는 이유)
- 모든 신규 기능은 additive·플래그/순수함수·파이프라인 미연결 → **프로덕션 알림 동작은 이 세션 내내 불변**
  (안정성 수정 #23/#31/#35 제외, 이들은 개선).
- 전 변경 테스트 동반(총 108케이스)·CI 자동화·검증 기록 문서화.
- 투자의견 금지·정형 우선·KST·이스케이프·키 비노출 규약 준수.
- 스키마 변경은 alembic·additive only → 롤백 시 데이터 손실 없음.
